/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */

import React from "react";

const MainContent = ({ children }) => {
  return <main className="main-content-section">{children}</main>;
};

export default MainContent;